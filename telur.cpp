#include <bits/stdc++.h>
using namespace std;

long long gcd(long long n){
    long long ans = 0;
    for(int i = 1; i*i < n; i++){
        if(n%i==0) ans++;
    }
    return (ans-1)*2;
}

long long g(int a, b){

}

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    long long n; cin >> n;
    for(int i = 2; < n; i++){
        if(n/2)
    }
}
