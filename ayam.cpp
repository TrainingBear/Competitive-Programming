#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n; cin >> n;
    string s1, s2;
    cin >> s1 >> s2;
    long long arr1[n];
    for(int i = 0; i< n; i++){
        cin >> arr1[i];
    }
    long long arr2[n];
    for(int i = 0; i< n; i++){
        cin >> arr2[i];
    }
}
