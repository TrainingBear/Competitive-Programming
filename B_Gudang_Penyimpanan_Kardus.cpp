#include <bits/stdc++.h>
using namespace std;
#define ll long long
static long long n, p, l;

void solve(){
    cin >> n >> p >> l;
    ll kardus = p*l;
    ll target = kardus*n;
    ll l = 0, r = sqrt(target)+1
}