#include <bits/stdc++.h>
using namespace std;

int t, n, m;
const int MAXN = 505;
string a[MAXN], b[MAXN];

void solve(){
    cin >> n >> m; 
    for(int i = 0; i < n; i++) cin >> a[i];
    for(int i = 0; i < n; i++) cin >> b[i];
}