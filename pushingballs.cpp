#include <bits/stdc++.h>
using namespace std;
void solve(){
    int n, m;
    cin >> n >> m;
    char grid[n][m];
    for(int i = 0; i < n; i++) cin >> grid[i];
    for(int i = 0; i < n; i++){
        bool flag = 0;
        for(int j = 0; j < m; j++){
           if(grid[i]!=1)
        }
    }
}
int main(){
    ios_base_sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t =1;
    cin >> t; while (t--) solve();
}
